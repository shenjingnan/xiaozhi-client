{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}",
    "pushArgs": ["--follow-tags"],
    "requireCleanWorkingDir": false,
    "requireUpstream": false,
    "commit": true,
    "tag": true,
    "push": true
  },
  "github": {
    "release": true,
    "releaseName": "🚀 v${version}",
    "releaseNotes": "npx conventional-changelog-cli -p conventionalcommits",
    "skipReleaseOnMissingReleaseNotes": false
  },
  "npm": {
    "publish": true,
    "publishArgs": ["--access", "public"],
    "skipChecks": true,
    "allowVersionBump": true
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "conventionalcommits",
      "infile": "CHANGELOG.md",
      "releaseCommitMessageFormat": "chore: release v${version}",
      "ignoreRecommendedBump": true,
      "strictSemVer": false
    }
  },
  "hooks": {
    "before:init": ["pnpm audit --audit-level moderate"],
    "after:bump": ["pnpm run build"],
    "before:release": "echo \"📦 准备发布版本 v${version}\"",
    "after:release": "echo \"🎉 版本 v${version} 发布完成！\""
  }
}
