name: NPM 发布

on:
  workflow_dispatch:
    inputs:
      version:
        description: "发布版本号 (例如: 1.0.0, 1.0.0-beta.1, patch, minor, major)"
        required: false
        default: ""
        type: string
      dry_run:
        description: "预演模式（仅预览，不实际发布）"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  npm-release:
    name: NPM 发布
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: 执行 NPM 发布流程
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          # 构建发布脚本参数
          RELEASE_ARGS=""

          # 添加版本号参数
          if [ -n "${{ github.event.inputs.version }}" ]; then
            RELEASE_ARGS="$RELEASE_ARGS ${{ github.event.inputs.version }}"
          fi

          # 添加选项参数
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            RELEASE_ARGS="$RELEASE_ARGS --dry-run"
          fi

          echo "🚀 执行发布脚本: npx tsx scripts/npm-release.ts$RELEASE_ARGS"
          npx tsx scripts/npm-release.ts$RELEASE_ARGS
