{
  "name": "xiaozhi-client",
  "version": "0.0.1",
  "description": "小智AI客户端，目前主要用于MCP的对接",
  "main": "dist/mcpWebSocketClient.js",
  "type": "module",
  "bin": {
    "xiaozhi": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "build:tsc": "tsc",
    "dev": "tsc --watch",
    "start": "npm run build && node dist/mcpWebSocketClient.js",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest",
    "prepublishOnly": "npm run build"
  },
  "author": "shenjingnan",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "chalk": "^5.4.1",
    "commander": "^14.0.0",
    "dotenv": "^16.5.0",
    "ora": "^8.2.0",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
