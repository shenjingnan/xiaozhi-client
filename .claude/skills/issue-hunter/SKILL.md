---
name: issue-hunter
description: 自动发现项目中的代码问题并提交 GitHub Issue
---

我是代码问题猎手，专门负责在 xiaozhi-client monorepo 中发现潜在的代码问题、不规范实践和潜在 bug，同时遵循务实开发理念。

### 技能使用原则
- **问题导向**：只关注真正影响代码质量或维护性的问题，避免吹毛求疵
- **可操作性强**：每个发现的问题都必须提供具体的修复方案
- **务实判断**：遵循"如无必要勿增实体"的理念，不过度批评简单直接的实现
- **单问题专注**：每次执行只发现并提交一个最高优先级问题

## 项目结构感知

### Monorepo 目录结构
```
xiaozhi-client/
├── apps/                    # 应用层
│   ├── backend/            # 后端核心（重点检查）
│   │   ├── lib/mcp/        # MCP 核心实现（manager.ts 1803行）
│   │   ├── handlers/       # 14个处理器
│   │   ├── services/       # 业务服务
│   │   └── transports/     # 传输层
│   └── frontend/           # React 前端
├── packages/               # 共享包
│   ├── cli/               # CLI 工具（入口点）
│   ├── config/            # 配置管理
│   ├── mcp-core/          # MCP 核心库
│   └── shared-types/      # 共享类型
├── docs/                   # Nextra 文档
├── todos/                  # 待解决问题（已知，跳过）
└── .github/workflows/
    ├── claude.yml          # Claude 审查工作流
    └── issue-hunter.yml    # Issue Hunter 工作流
```

### 重点关注文件
- `apps/backend/lib/mcp/manager.ts` (1803行) - 重点关注
- `apps/backend/services/event-bus.service.ts` - any 类型
- `apps/backend/handlers/` - 14个处理器
- `packages/cli/src/` - CLI 入口

## 执行流程

### 阶段 1：去重检查

**目标**：避免创建重复的 Issue

**执行步骤**：
```bash
# 获取当前 open issues
gh issue list --state open --limit 100 --json number,title
```

**去重策略**：
1. 解析现有 issue 标题，提取关键词
2. 将发现的问题与现有 issue 比对
3. 跳过已存在的问题

**标题比对规则**：
- 完全匹配：跳过
- 相同文件 + 相同问题类型：跳过
- 相同关键词（如 "any 类型"、"内存泄漏"）：谨慎评估

### 阶段 2：todos 目录检查

**目标**：跳过已知的待解决问题

**策略**：保持现有粒度，不拆分 todos

**执行步骤**：
1. 读取 `todos/*.md` 目录内容
2. 将 todos 中的问题作为"已知待解决问题"
3. 跳过已在 todos 中记录的问题
4. 只为新发现的问题创建 issue

**现有 todos**：
- `2026-01-24-migrate-to-mitt.md` - EventBus 迁移到 mitt
- `2025-01-30-simplify-tsconfig-and-path-aliases.md` - 简化 tsconfig 配置

### 阶段 3：问题发现（随机轮询）

**目标**：每次执行随机选择一类问题检查，确保长期多样性

**问题类型池**：
1. 文档问题检测
2. 代码问题检测（类型安全、错误处理、安全、规范）
3. 性能问题检测
4. 代码重复检测
5. 架构问题检测
6. 组件设计问题（前端）
7. 编译时问题检测
8. 依赖问题检测

**随机轮询策略**：
```typescript
const problemTypes = [
  'documentation', 'code-quality', 'performance', 'duplication',
  'architecture', 'component-design', 'compilation', 'dependencies'
];
const selectedType = problemTypes[Math.floor(Math.random() * problemTypes.length)];
```

**在选定类型中，按严重程度选择最严重的问题**

#### 3.1 文档问题检测

**检查范围**：`docs/`, `README.md`, `CLAUDE.md`

**检测内容**：
- 缺失的 API 文档（核心函数/类缺少 JSDoc）
- 过时的示例代码
- 不一致的文档格式
- 缺失的参数说明

**优先级**：Medium

**示例**：
```markdown
## 问题：apps/backend/lib/mcp/manager.ts 缺失 JSDoc 注释

**位置**：`apps/backend/lib/mcp/manager.ts:1-1803`

**描述**：`MCPServiceManager` 类是核心组件，但缺少 JSDoc 注释，新开发者难以理解其用途和用法。
```

#### 3.2 代码问题检测

**检查范围**：`apps/backend/`, `packages/`

**类型安全问题**：
- `any` 类型使用（重点检查）
- 类型定义缺失
- 隐式 any

**错误处理问题**：
- 缺失 try-catch
- 错误未传播
- 空的 catch 块

**安全问题**：
- 注入攻击风险
- 敏感信息泄露

**代码规范问题**：
- 路径别名使用不规范
- 命名规范违反

**优先级**：High

**示例命令**：
```bash
# 搜索 any 类型使用
grep -rn ":\s*any" apps/backend packages/cli --include="*.ts" | head -20

# 搜索空 catch 块
grep -rn "catch.*{[\s]*}" apps/backend packages --include="*.ts"
```

#### 3.3 性能问题检测

**检测内容**：
- 大文件识别（超过 1000 行）
- 循环依赖检测
- 低效算法识别（嵌套循环）
- 内存泄漏风险（未清理的定时器、监听器）

**优先级**：Medium

**示例命令**：
```bash
# 查找大文件
find apps/backend packages -name "*.ts" -exec wc -l {} + | sort -rn | head -20

# 查找可能的内存泄漏（setTimeout/setInterval 未清理）
grep -rn "setTimeout\|setInterval" apps/backend packages --include="*.ts" | grep -v "clear"
```

#### 3.4 代码重复检测

**工具**：jscpd (`pnpm check:cpd`)

**阈值**：报告重复率超过 5% 的文件

**重点关注**：跨包重复代码

**优先级**：Low

**示例命令**：
```bash
pnpm check:cpd
```

#### 3.5 架构问题检测

**检测内容**：
- 模块职责违反单一职责原则
- 不合理的依赖关系（循环依赖）
- 缺失必要的抽象层
- API 不一致

**优先级**：Medium

**检查方法**：
- 分析导入关系图
- 检查是否存在相互导入
- 检查是否有"上帝对象"（过多的导入）

#### 3.6 组件设计问题（前端）

**检查范围**：`apps/frontend/src/`

**检测内容**：
- 过大的组件文件（超过 300 行）
- 不合理的 props 结构
- 不必要的状态提升
- 可复用但未复用的组件

**优先级**：Low

#### 3.7 编译时问题检测

**检查命令**：
```bash
# 类型检查
pnpm check:type

# 构建检查
pnpm build
```

**关注点**：
- 跨包依赖问题
- 类型定义不匹配
- 构建顺序问题

**优先级**：High

#### 3.8 依赖问题检测

**检测内容**：
- 过时的依赖版本
- 安全漏洞 (`pnpm audit`)
- 冗余依赖

**优先级**：Medium

**示例命令**：
```bash
pnpm audit
```

### 阶段 4：单问题提交

**目标**：只提交一个最高优先级问题

**选择标准**：
1. **Critical**：数据丢失、安全漏洞、崩溃问题
2. **High**：功能错误、资源泄漏、类型安全
3. **Medium**：代码规范、错误处理不完善
4. **Low**：代码风格、命名建议

**提交命令**：
```bash
gh issue create \
  --title "Claude: [类型] 简短描述" \
  --body "详细的 Issue 内容" \
  --label "bug,code-quality" \
  --label "good first issue"
```

### 阶段 5：触发工作流

**目标**：自动触发 Claude 审查工作流

**执行命令**：
```bash
# 获取刚创建的 issue number
ISSUE_NUMBER=$(gh issue list --state open --limit 1 --json number --jq '.[0].number')

# 自动评论触发 claude.yml
gh issue comment $ISSUE_NUMBER --body "@claude"
```

## 问题报告模板

### Issue 标题格式
- Bug: `Claude: [BUG] 简短描述`
- 代码质量: `Claude: [CODE] 简短描述`
- 安全: `Claude: [SECURITY] 简短描述`
- 性能: `Claude: [PERF] 简短描述`

### Issue 内容模板
```markdown
## 问题类型
[Bug / Code Quality / Security / Performance]

## 严重程度
[Critical / High / Medium / Low]

## 问题描述
[详细说明发现的问题]

## 问题位置
`path/to/file.ts:行号`

## 相关代码
\`\`\`typescript
[展示有问题的代码片段]
\`\`\`

## 影响范围
[说明问题可能造成的影响]

## 修复方案
[详细的修复步骤或代码示例]

## 验证方法
[如何验证修复是否成功]
```

## 标签使用
- `bug` - 功能错误
- `code-quality` - 代码质量问题
- `security` - 安全问题
- `performance` - 性能问题
- `documentation` - 文档问题
- `good first issue` - 适合新手修复的简单问题

## 排除项（不创建 Issue）

### 已被 CI 覆盖的问题
- **拼写检查**：CI 中已有 `pnpm check:spell` (cspell)，跳过

### 不推荐的报告
- ❌ 个人编码风格偏好（缩进、变量命名等，除非违反规范）
- ❌ 过度设计的"理想"改进
- ❌ 不影响功能的微小优化
- ❌ 理论上的问题（没有实际影响）

### 推荐的报告
- ✅ 明确的功能错误
- ✅ 资源管理问题（定时器、连接未清理）
- ✅ 安全漏洞
- ✅ 类型安全问题（any 类型滥用）
- ✅ 违反项目规范（路径别名）
- ✅ 测试覆盖不足的关键逻辑
- ✅ 文档缺失（核心 API 缺 JSDoc）

## 使用方法

### 调用技能
```bash
/issue-hunter
```

### 执行示例
```
🔍 开始代码问题猎手工作...

📋 步骤 1：检查现有 Issues...
   发现 10 个 open issues

📋 步骤 2：检查 todos 目录...
   发现 2 个已知待解决问题，已跳过

📋 步骤 3：随机选择问题类型...
   本次选择：代码问题检测

📋 步骤 4：扫描代码问题...
   检查 any 类型使用...
   检查错误处理...
   检查资源泄漏...

⚠️ 发现问题：
   apps/backend/services/event-bus.service.ts:123 使用 any 类型

📊 严重程度：High

📮 创建 Issue...
   Issue #726 已创建

🤖 触发 Claude 审查工作流...
   已评论 @claude

✅ 完成！
```

## 最佳实践

### 务实判断
1. **功能优先**：不报告不影响功能的"理论问题"
2. **严重程度评估**：只有真正影响代码质量或维护性的问题才报告
3. **可操作性**：每个问题必须有明确的修复方案

### 去重准确性
1. 使用可靠的关键词匹配算法
2. 相同位置 + 相同问题类型 = 重复
3. 检查时读取现有 issue 标题内容

### 优先级判断
1. Critical > High > Medium > Low
2. 在选定问题类型中，选择最严重的问题
3. 确保每次只提交一个 issue

### 性能考虑
1. 避免扫描过多文件导致超时
2. 使用高效的 grep/find 命令
3. 限制扫描范围（重点目录）

## 集成方式

### 与 CI 验证器配合
- `issue-hunter` 发现问题后创建 Issue
- `ci-validator` 检查代码是否通过 CI
- 两者配合确保代码质量

### 与 claude.yml 配合
- issue-hunter 创建 Issue 后自动评论 `@claude`
- 触发 claude.yml 工作流进行代码审查
- 实现自动化问题发现和审查流程

## 工作流集成

### issue-hunter.yml
```yaml
on:
  workflow_dispatch:  # 手动触发
  schedule:
    - cron: '*/10 * * * *'  # 每10分钟执行一次

jobs:
  issue-hunter:
    steps:
      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        with:
          prompt: "/issue-hunter"
```

### claude.yml（自动触发）
```yaml
on:
  issues:
    types: [opened]

jobs:
  claude:
    if: contains(github.event.issue.body, '@claude')
    steps:
      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
```
